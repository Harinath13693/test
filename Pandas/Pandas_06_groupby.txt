import pandas as pd

df=pd.read_csv('path_file')

g=df.groupby(column_name)
---creates a dataframes by column_name (spilt apply combine)

maxdf=g.max()
--it contains maxvalue of each column group by column_name

avgdf=g.mean()
--it contains averageValue of each column group by column_name

descdf=g.describe()
--displays all measures group by column_name

https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.groupby.html